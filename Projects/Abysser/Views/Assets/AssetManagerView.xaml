<UserControl 
    x:Class="Abysser.Views.Assets.AssetManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBlock Text="Asset Manager"/>
        <StackPanel Orientation="Horizontal">
            <Button 
                Content="textures"
                Click="TexturesButtonClick"/>
            <Button 
                Content="triggers"
                Click="TriggersButtonClick"/>
        </StackPanel>
        
        <ListBox ItemsSource="{Binding Textures}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Description}"/>
                        <Image Source="{Binding Texture}" Width="64" Height="64"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
